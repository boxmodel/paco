{
  "name": "paco",
  "private": true,
  "version": "0.0.0",
  "description": "An AngularJS, Firebase, Vimeo brainstorm.",
  "repository": "https://github.com/angular/paco",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
